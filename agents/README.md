# Agent operating system

Policies state boundaries; templates make bounded implementation/review handoffs repeatable; debts record only real open work. `policies/ISSUE_GOVERNANCE.md` applies the canonical public Issue policy before an agent publishes backlog items. This tree is internal operational guidance and is excluded from the packaged Docs corpus.
