# Changes from upstream

This preview adds ThreadCells public branding and canonical commands, compatible `cao*` entry points, a packaged documentation reader, public operator documentation, and release-preparation artifacts. It intentionally retains the established Python import namespace and protocol/state identifiers for compatibility.
