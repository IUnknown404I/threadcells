# Branding

Use the display casing **ThreadCells** and the category “self-hosted coding-agent operations console.” The validated canonical raster pack is tracked under `brand/threadcells_brand_asset_pack/`; its `README.md` and `SHA256SUMS.txt` define the supplied inventory and integrity authority. The standalone symbol is unchanged from the pre-release product, while the horizontal wordmark names ThreadCells. No vector availability or independent artwork-license clearance is claimed.
